import React from "react";
import Topbar from "./Topbar";
import NavBar from "./NavBar";

const Header = () => {
    return (
        <header>
            <Topbar />
            <NavBar />
        </header>
    );
};

export default Header;
